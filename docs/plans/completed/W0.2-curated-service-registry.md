# W0.2 — Curated service registry: data model + read API + seed from W−1.4

Part of the [pivot plan](README.md). Workstream 0. Replaces per-request Bazaar discovery on the hot path.

**Workstream:** 0
**Dependencies:** W0.1, W−1.4
**Status:** completed

## Acceptance criteria

- `service_registry` table is populated from `data/call_recipes.json`.
- `src/registry/read.ts` exposes `getActiveServices(category): Promise<RegistryEntry[]>` and `getRecipe(service_id): Promise<CallRecipe>`.
- All initial seeded entries have status `active`, score 1.0, last_vetted_at = now.

## Files

- `src/registry/read.ts` (new)
- `src/registry/types.ts` (new)
- `scripts/seed-registry.ts` (new)

## Validation

- `~/.deno/bin/deno check src/registry/*.ts scripts/seed-registry.ts`
- `~/.deno/bin/deno run --allow-net --allow-env --allow-read scripts/seed-registry.ts` populates the table.

## Test spec

- `src/registry/read_test.ts` — seed, fetch by category, assert returned entries match seed.
