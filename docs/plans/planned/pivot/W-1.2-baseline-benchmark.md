# W−1.2 — Baseline benchmark script + persisted v8 baseline

Part of the [pivot plan](README.md). Workstream −1 (Telemetry baseline).

**Workstream:** −1
**Dependencies:** W−1.1
**Status:** planned

## Acceptance criteria

- New `scripts/benchmark.ts` runs the fixture suite (`src/fixtures/wallets.ts`) end-to-end against real services.
- Output `benchmarks/v8-baseline.json` contains per-wallet: total `duration_ms`, per-phase `duration_ms`, per-service `cost_usd`, total `cost_usd`, final verdict + accuracy match.
- Aggregate section: P50, P95 latency; mean and P95 cost; accuracy (n/9 correct).
- Runnable via `deno task benchmark`.

## Files

- `scripts/benchmark.ts` (new)
- `benchmarks/v8-baseline.json` (new, committed)
- `deno.json` — add `benchmark` task

## Validation

- `~/.deno/bin/deno check scripts/benchmark.ts`
- `~/.deno/bin/deno task benchmark` — produces the baseline file and exits 0.

## Test spec

- Acceptance: re-running benchmark twice produces accuracy ≥ 8/9 both runs (allow for one transient).
- Acceptance: P95 latency is recorded and non-zero.
