# W3.1 — Status page with latency + uptime

Part of the [pivot plan](README.md). Workstream 3 (Productization). Unglamorous but expected by enterprise buyers.

**Workstream:** 3
**Dependencies:** W1.2 (usage_events feed)
**Status:** planned

## Acceptance criteria

- Public status page (Cloudflare Pages + Workers or hosted Statuspage). Shows current uptime, P50/P95 verify latency over last 24h.
- Data sourced from `usage_events` via a periodic export.

## Files

- `status/` (new directory or hosted-service config)
- `scripts/export-status.ts` (new)

## Validation

- Manual: visit status URL, verify metrics render.

## Test spec

- Manual smoke: degrade a downstream service in staging, verify status page reflects it.
