# W3.2 — Docs site

Part of the [pivot plan](README.md). Workstream 3.

**Workstream:** 3
**Dependencies:** W1.4 (schema is locked)
**Status:** planned

## Acceptance criteria

- Public docs covering: API auth (bearer + x402), all HTTP routes with examples, MCP tool reference, `WalletVerdict` schema, error codes, rate limits.
- Hosted at `docs.<domain>` or `<domain>/docs`. Auto-built from markdown.

## Files

- `docs/site/` (new — Vitepress / Mintlify / Docusaurus, pick at execution time)

## Validation

- Manual: visit each documented route and confirm example works.

## Test spec

- N/A (docs).
