# W1.4 — Hardened verdict contract: `schema_version`

Part of the [pivot plan](README.md). Workstream 1. Independent of other W1 tickets — can run early to lock the public API contract.

**Workstream:** 1
**Dependencies:** none (can run early)
**Status:** planned

## Acceptance criteria

- `WalletVerdict` (`src/agent/verdict.ts`) gains a top-level `schema_version: "1"` string.
- Public docs name v1 as the supported version. Older clients ignoring the field is documented.
- Versioning policy doc in `docs/verdict-schema.md`.

## Files

- `src/agent/verdict.ts`
- `docs/verdict-schema.md` (new)

## Validation

- `~/.deno/bin/deno check src/agent/verdict.ts`
- `~/.deno/bin/deno task test:unit`

## Test spec

- Test: every verdict produced by `verify()` has `schema_version === "1"`.
