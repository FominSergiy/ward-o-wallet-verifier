# W2.4 — Internal v0 service score endpoint (admin-only)

Part of the [pivot plan](README.md). Workstream 2. Internal-only first version of what eventually becomes the public Product B endpoint.

**Workstream:** 2
**Dependencies:** W0.9, W2.1, W2.3
**Status:** planned

## Acceptance criteria

- New admin-gated endpoint `GET /admin/score/:service_id` returns the score breakdown (reliability, latency, coverage, verdict contribution rate).
- Used internally to validate the score machine before any public release of Product B.

## Files

- `src/routes/admin/score.ts` (new)

## Validation

- `~/.deno/bin/deno check src/routes/admin/score.ts`

## Test spec

- Test: admin bearer returns score; non-admin → 403.
