# W4.0 — Schema forward-compatibility audit (the only active W4 ticket)

Part of the [pivot plan](README.md). Workstream 4 (Long-horizon graph model). Real implementation is gated on accumulating outcome data from real agent traffic; until then this audit is the only active work.

**Workstream:** 4
**Dependencies:** W2.1, W2.2
**Status:** planned

## Acceptance criteria

- Document `docs/graph-model-data-requirements.md` listing what the eventual classifier needs: positive/negative labels from `agent_outcomes`, address-level features derivable from `service_observations`, label volume thresholds.
- Audit `service_observations` + `agent_outcomes` + `usage_events` schemas to confirm nothing prevents the classifier from being trained against them. List any gaps as follow-up tickets (do NOT implement them now).

## Files

- `docs/graph-model-data-requirements.md` (new)

## Validation

- Manual review against [docs/plans/planned/onchain-graph-risk-model.md](../onchain-graph-risk-model.md).

## Test spec

- N/A (design audit).
