# Agent Log

Append-only. One row per completed feature, newest at bottom. Cross-reference slug with `docs/features/<slug>.md`.

| Date       | Slug          | Summary                                                                         |
|------------|---------------|---------------------------------------------------------------------------------|
| 2026-05-21 | x402-payments | agnicFetch x402 proxy client + x402Invoker replacing all stubs in budgetedCall |
| 2026-05-21 | service-discovery | CDP x402 discovery pipeline + LLM rerank + POST /discover route + e2e test     |
| 2026-05-22 | pattern-adapter-shape-trim | Trim alternateBodyShapes 5→1 to cut paid-call volume; v3 e2e validation 2/2 match, 0 rate-limit hits |
| 2026-05-22 | durable-health-filter | Persist lastErrorCode in health store; rank filters services flagged with payment_exceeds_max (durable catalog↔runtime price drift) |
| 2026-05-22 | preflight-budget-check | /verify-agent calls Agnic /api/balance before running the DAG; returns HTTP 503 budget_exhausted if totalBalance < AGNIC_BUDGET_MIN_USD (default $0.10) |
| 2026-05-22 | legacy-rip-out | Delete /verify + /plan routes, src/dag/** stub DAG, USE_DISCOVERY legacy branch and 7 dependent modules; relocate shared types to src/agent/types.ts |
| 2026-05-23 | verify-agent-stream | Add VerifyEvent union + thread optional onEvent through verifyAgent / invokeAll / discover; POST /verify-agent-stream SSE route mirroring /verify-agent error mapping. Backend half of the demo-ui plan. |
| 2026-05-23 | demo-ui | Vite+React single-page demo under web/: Logo / InputForm / PlanCard (saved to localStorage) / LogStream (SSE) / VerdictCard. E2E verified in browser against vitalik.eth — Plan → Save → reload-restore → Execute → live phase/service events → "safe to transact" verdict. |
| 2026-05-23 | discover-stream | POST /discover-stream SSE route + two-tab terminal UI. Frontend Plan now streams discover events (phase/log/plan) into a "plan" tab, Execute into an "execute" tab — both preserved, click to flip without re-running. PlanEvent gained optional `unresolvedCategories`; api.ts extracted a generic `consumeSSE` helper. |
| 2026-05-23 | discovery-quality-and-not-found-fix | Refine labels/web_sentiment/contract_analysis CDP queries; EOA short-circuit drops contract_analysis when address has no bytecode (new `coverage.not_applicable` bucket); constrain LLM-fallback adapter to catalog URL path (validator + rewrite); normalize agnic error codes to snake_case and add `not_found` to durable-block codes. v6 e2e: 12/12 verdicts correct across 3 runs × 4 wallets, 0 `Not found` errors (v5 baseline: 30%). |
| 2026-05-23 | flow-viz | Pure-SVG flow diagram view on Execute tab — visualizes verify-agent fanout into categories → x402 payment diamonds → fallback diamonds (on primary error) → synthesize → verdict, derived from `VerifyEvent[]` via a `useFlowState` hook. Toggle (`logs ⇄ flow`) in the existing tab strip. |
| 2026-05-23 | v6-followups-and-orbis-probe | Wrap agnicFetch json-parse in try/catch (synthetic `non_json_response` code); add adapter_build_failed / adapter_call_failed / adapter_llm_build_failed codes; surface errorCode in route receipts; add dev-only FORCE_LLM_ADAPTER=true env flag + 2-wallet stress run (LLM path 8/8, 0 validator rewrites, verdicts still correct). Orbis probe revealed labels/reputation services were returning service-descriptor blobs, not address data — the pattern adapter is hitting the root URL when real data lives at `/label` and `/score` sub-paths. |
| 2026-05-23 | ui-wardo-styling | Ward-o mascot pass: hand-authored SVG mascot (neutral/safe/villain) in header + verdict card, plus pixel-art tamagotchi above the flow/logs panel — idle when waiting, sprite-sheet walk animation while plan or verify stream is active. |
| 2026-05-23 | adapter-descriptor-retry | Detect service-descriptor responses (`{endpoints[]}` shape) after a successful pattern call; retry once against the first non-info action sub-endpoint (category-hinted picker for labels/web_sentiment/sanctions), recording `adapterPath: "pattern+subpath"`. Validated end-to-end on 4 known wallets — both orbisapi labels + reputation services now hit 100% with substantive label/score payloads instead of API metadata; verdicts 4/4 match expected. |
| 2026-05-23 | pitch-deck-business | Business-side pitch deck at `docs/WARD-o-business.html` (alongside the existing technical deck). 9 slides, ~3-min runtime, story-led ("seatbelt for agents that spend money"), with ICP/persona, benefits-not-features cards, competitive 2×2, TAM/SAM/SOM, GTM, and ask. Speaker notes embedded with `?notes` toggle. Reuses palette + mascot SVGs from `assets/`. |
| 2026-05-23 | pitch-deck-workshop2 | Workshop #2 integration into the business deck: replaced Slide 6 (Why-Now tech timeline) with an adoption staircase (devs today → companies → consumer apps → mass-market crypto-via-AI), added Stripe/Twilio playbook one-liner to Slide 8 GTM. Speaker notes deepened on Slides 1/2/4/6/8 with real-world citations (Grok/Bankr, CoinDesk, TRM Labs), guardrail-as-backup framing, and Stripe/Twilio comparables. Deck stays at 9 slides; no time-budget impact. |
| 2026-05-23 | mcp-server | Expose `verifyAgent` as an MCP server with a single `verify_wallet` tool (address + chain + optional budgetCeiling/categories). Two transports share the same factory: stdio (`src/mcp/stdio.ts`) for local agent integrations and WebStandard Streamable HTTP (`src/mcp/http.ts`, port 9765) for hosted use. E2E verified via `scripts/mcp_e2e.ts` against vitalik.eth on eth — verdict `safe_to_transact`, $0.0174 USDC spent in 45s; HTTP `initialize` over curl returns server info + session id. |
| 2026-05-23 | ui-flow-polish | Flow view on plan tab (default flow on both tabs); flow diagram bumped to 480px (logs stay at 320px); cross-mode state flushed only when the wallet address changes — plan persists through execute for the same address. |
| 2026-05-23 | synthesis-signal-lift | Chainalysis sanctions-oracle on-chain hard-gate (chain primitive, free RPC read) + labels-discovery quality lift (sharper query, description-keyword rerank bump, post-call quality-probe with durable demotion) + viem ENS reverse resolution added to DEFAULT_CATEGORIES. T1 and T3 framed as chain primitives, not vendor URLs — verified BlockSec/MetaSleuth is not in CDP Bazaar, so pinning their URL was rejected to preserve the self-discovery pitch. v7 regression vs v6_3 baseline: 9/9 strict match, Lazarus 36s→2.7s and $0 spend, Vitalik/Binance medium→high confidence, no regressions. |
| 2026-05-23 | drop-chain-selector-and-surface-direct-paths | Drop UI chain selector; backend fans out Chainalysis oracle across all 5 EVM chains (strictest signal wins) and defaults other paths to eth. Fixes reported bug where `0x7F367cC41522cE07553e823bf3be79A889DEbe1B` returned safe on base but flagged on eth. Non-EVM addresses (Solana, Bitcoin) rejected with 400. Service events added to ENS resolver; `kind:"direct"` flag distinguishes free chain primitives from paid x402 calls. Flow diagram renders direct paths as dashed circles beneath each category. |
| 2026-05-23 | labels-registry-hybrid | Hybrid x402+hardcoded labels layer: `fetchLabelsRegistry` calls `https://eth-labels.com/labels/<address>` (dawsbot/eth-labels, free public API, ~170k labeled addresses) in parallel with the x402 labeler and ENS; result merges into `findings.labels` as `{x402_result, registry}` (same shape pattern as the Chainalysis oracle's merge into `findings.sanctions`). Synthesis prompt extended with CEX allow-list (coinbase, binance, kraken, …) and registry-negative blocklist (blocked, ofac-sanctioned, tornado-cash, …). v8 e2e vs v7: 9/9 strict match preserved, **Coinbase 1 and Kraken 4 lifted medium→high** with Opus reasoning explicitly citing registry attribution; spend down 38% ($0.1218→$0.0753); zero regressions across the other 7 fixtures. Both layers always run — registry is supplementary, never replaces self-discovery. |
| 2026-05-23 | ui-transparency-pass | UI now reflects what the agent actually does: PlanCard gained an "Always-on free checks" section showing Chainalysis oracle, eth-labels.com, ENS resolver at $0 with rationale tooltips (sourced from a new `buildDeterministicSources` builder; emitted via `DiscoveryPlan.deterministicSources` and the SSE `plan` event — no behavior change in `verify.ts`). VerdictCard receipts now render an `adapterPath` badge (`pattern`/`pattern+subpath`/`llm`) so the orbisapi descriptor-retry is visible. Every category chip in PlanCard and VerdictCard carries a native `title` tooltip from a new `web/src/categoryLabels.ts` hint map. End-to-end smoke on vitalik.eth: plan shows 5 paid + 3 free rows ($0.042 total), verdict shows pattern+subpath on the orbisapi labeler and llm on the web_sentiment fallback — verdict `safe_to_transact`. |
| 2026-05-23 | deploy-mcp-pipeline | mount MCP /mcp on main Hono app + bearer auth via MCP_SHARED_SECRET; restored on feat/deploy-pipeline so /mcp ships with the same Deno Deploy push as REST |
| 2026-05-24 | reliability-fixes-web-sentiment-ens-drop-contract-analysis | Drop `contract_analysis` category entirely (was always EOA-skipped or had no Bazaar provider); add `assertNoUnsubstitutedPlaceholders` URL validator in adapter so literal `:foo` placeholders fail fast as `unsubstituted_path_param` instead of going out as broken HTML 404s; expand `DURABLE_BLOCK_CODES` to add `unsubstituted_path_param` / `descriptor_only_response` / `non_json_response` for one-strike block on structural failures; surface ENS service events with `durationMs` + always-emitted `ens_resolve:` log line (LogStream renders `kind:"direct"` events as `resolved · {ms}` instead of `paid $0.0000`); add host-diversity hint in the rerank prompt + tighten `web_sentiment` / `onchain_history` queries to push the ranker off Orbis monoculture. E2E on vitalik.eth: bad Orbis `:endpoint` variant caught pre-call, alternate-rescue picks `wallet-address-risk-api-c6680c`, verdict `safe_to_transact` high-confidence, total spend $0.0109. |
| 2026-05-24 | ui-polish-flow-diagram | Cosmetic + correctness pass on the three demo cards. PlanCard: dashed divider + extra spacing between paid services and the "Always-on free checks" block. VerdictCard: three uppercase section headers (Summary / Category findings / Paid services breakdown) so the structure is scannable. FlowDiagram: bump `DIRECT_SPACING` 26→38 (chain labels no longer blur), wrap each sub-fan-out (sanctions 5 chains, ENS) in a dashed container rect so the row reads as one widget belonging to its parent category. Bug fixes: ENS edge no longer renders red on success — direct-only category status now promotes `active→ok` on the per-event handler and the invoke-end cascade correctly flips direct-only `active→ok` instead of `→error`. Fallback flow now renders as a single chain (`primary →(red)→ fallback →(green)→ synth`); the parallel `primary→synth` edge is suppressed entirely when a fallback exists. Verified against `0xdAD8…D96` (bob-the-builder.eth) with real backend: PlanCard divider visible, three verdict headers in DOM, ENS direct-node + edge both green, 15 edges total (down from 17 — the two suppressed are pay→synth when fallback exists). |
| 2026-06-05 | validation-harness | Agent-validation tooling pass (no runtime change). `deno task check` now globs `src/**/*.ts` instead of two entrypoints, so type errors in any module/test are caught. Added `test:unit` (offline-safe alias) + `test:e2e` (`RUN_E2E=1`) tasks and documented the paid-route gate in CLAUDE.md. New `src/fixtures/wallets.ts` — typed canonical address→verdict cases (9 wallets from `report_v8.md`) + `wallets_test.ts` invariants. Added a scoped `PostToolUse` (Edit\|Write) hook to committed `.claude/settings.json` that runs `deno fmt`+`lint` on **only the edited `.ts` file** (a tree-wide `deno fmt src/` was rejected after it reformatted 40+ unrelated files). Verified: check/lint clean, `test:unit` 253 passed / 5 ignored. |
| 2026-06-16 | event-schema-overhaul | W-1.1: PhaseEvent + ServiceEvent gain required `request_id`, `duration_ms`; ServiceEvent gains `cost_usd: number | null`; LogEvent is a discriminated union requiring `code` on error-level logs; `request_id` generated at route layer and threaded through verifyAgent → invokeAll → discover. 257 unit tests passing. |
| 2026-06-16 | cassette-replay-tests | Cassette-based offline regression suite for verifyAgent. `scripts/record-cassettes.ts` runs the full pipeline against each fixture wallet and captures all HTTP interactions to `tests/cassettes/<address>.json`. `src/testing/fetch_interceptor.ts` provides recording + FIFO-replay interceptors that patch `globalThis.fetch`. `deno task test:replay` replays all 9 cassettes offline (HEALTH_TRACKING=false) and asserts 9/9 verdict accuracy in 37ms with zero paid calls. `deno task cassette:record` re-records when the pipeline changes. |
| 2026-06-17 | postgres-infra | W0.1: Postgres infrastructure on Neon (managed serverless PG; no Docker — local dev uses a Neon dev branch, prod the pooled endpoint). Driver = `npm:postgres` reached only via `getDb()` in `src/db/client.ts`, which is a no-op when `DATABASE_URL` is unset so `deno task test` stays offline. `db/migrations/0001_init.sql` creates six tables (tenants, api_keys, usage_events, service_registry, service_observations, service_health_durable) forward-compatible with W1/W2/W4 (nullable stripe_customer_id, severity_contribution, outcome_label; service_health_durable mirrors the ServiceHealth interface). `scripts/migrate.ts` is a forward-only runner tracked in `schema_migrations`, wired to `deno task db:migrate`. Row types in `src/db/types.ts`. DB conventions locked in CLAUDE.md; Neon provisioning documented in docs/deployment.md. Offline test run: 1 passed / 2 ignored (DB-gated). |
| 2026-06-17 | postgres-provision | W0.1 follow-up: live-provisioned the Neon project + dev branch, set `DATABASE_URL` in local `.env`, applied `0001_init.sql` against the dev branch, and ran the DB-gated tests live (3/3 pass, 0 ignored). Standardized on the Neon **pooled** (`-pooler`) endpoint for both local and prod (one config style; local exercises the same pooler path as prod) and added `prepare: false` to both `postgres()` call sites (`src/db/client.ts`, `scripts/migrate.ts`) for PgBouncer transaction-mode compatibility. Decided **no Neon Auth** — caller auth is API-key based via the `tenants`/`api_keys` tables (W1.1), keeping the schema host-portable. `.env.example` + `docs/deployment.md` updated to pooled-everywhere. |
| 2026-06-17 | health-db-probe | `/health` now probes the database and returns `db: "ok" \| "disabled" \| "error"` (was static `{status:"ok"}`), so a deploy can verify the Postgres wiring with one curl instead of waiting for a route to need it. `src/main.ts` exports `app` and guards `Deno.serve` behind `import.meta.main` so it's testable; new `src/main_test.ts` covers the offline (`disabled`) case + a `DATABASE_URL`-gated live (`ok`) case. Deployment smoke docs updated to the new shape. Verified live against the dev branch: 2/2 pass. |
| 2026-06-17 | service-observations-writer | W0.8: `src/observability/observations.ts` — fire-and-forget writer that inserts a row into `service_observations` for every terminal ServiceEvent (ok/error/fallback) emitted by the invoke pipeline. Hooked into `src/agent/invoke_all.ts` at all four terminal emit sites (x402 ok, x402 error/fallback, viem ok, viem error). Write failures are caught+logged and never propagate. 6 new unit tests; 277 total passing. |
| 2026-06-17 | w0.3-durable-health-store | Migrated service health store from JSON/in-memory to Postgres service_health_durable table |
| 2026-06-17 | w0.2-service-registry | W0.2: read API (`src/registry/read.ts` — `getActiveServices`/`getRecipe`) + seed script (`scripts/seed-registry.ts` from `data/call_recipes.json`) over the curated `service_registry` table; migration 0002 adds `score`/`last_vetted_at`. Docs bookkeeping backfilled after the fact (PR shipped code only). |
| 2026-06-17 | w0.4-hot-path-discovery-swap | W0.4: the verify request path no longer calls `discover()` (Bazaar fanout + LLM rerank). New `src/registry/select.ts#selectFromRegistry()` builds the `DiscoveryPlan` by reading the curated `service_registry` (top scorer per category as primary, rest as alternates), joining call shapes from `data/call_recipes.json` via `loadAllRecipes()` (new in `read.ts`). Offline-safe: DB read failure/empty → falls back to the recipe file at score 1.0. `src/agent/verify.ts` swapped to `selectFromRegistry` (phase label kept "discover" for SSE/UI parity); `discover()` preserved for snapshot-recipes + the future W0.10 vetter. Validated with one real paid pass over all 9 fixtures: **9/9 verdict parity**, discover→select phase **~9.2s mean → 0.2s** (per-wallet 11–13s → 64–92ms), total latency ~16.8s mean; cassettes re-recorded so `test:replay` is green (9/9 offline, zero Bazaar). Added `verify` injection seam to the verify-agent router so hermetic budget-guard tests don't leak real network. 272 tests pass. Known gaps: live Neon branch missing migration 0002 (`score` column) so prod currently uses the recipe-file fallback; `web_sentiment` recipe price drifted (always fails, verdict unaffected). |
| 2026-06-20 | w0.9-score-based-ranker | W0.9: deterministic score-based ranker. `src/registry/score.ts` exposes `recomputeScores()` — reads trailing-30-day `service_observations`, computes reliability/P95-latency/coverage score (weights 0.6/0.25/0.15), and writes updated `score` + `status` back to `service_registry`. Status machine: active→probation (<50% reliability), probation→active (≥80%), probation→blocked (<20%), blocked→blocked (operator-only lift). `selectFromRegistry` gained an explicit blocked-guard so no blocked service can be selected even via test seams. 16 new unit tests (score_test.ts); 23 registry tests total, all passing. |
| 2026-06-20 | w0.6-hot-path-synthesis-swap | W0.6: synthesis defaults to `anthropic/claude-haiku-4.5`; routes to `anthropic/claude-opus-4.7` only when findings contain risk keywords (tornado, scam, mixer, etc.), coverage <50%, or ≤1 category resolved. 5/7 synthesis-calling fixture wallets use Haiku; 1/7 (Tornado Cash) routes to Opus. Estimated 68.6% synthesis cost reduction vs v8 baseline. 9/9 replay, 294 unit tests passing. |
| 2026-06-20 | w0.5-per-call-timeouts-caching | W0.5: 2s per-call timeout in `invokeAll` (configurable via `INVOKE_TIMEOUT_MS`; timeout → error outcome → fall through to alternate); 24h ENS cache keyed by `(address, chain)` and eth-labels cache keyed by `address` (TTLs configurable via `ENS_CACHE_TTL_MS`/`ETH_LABELS_CACHE_TTL_MS`); `src/cache/kv.ts` abstraction picks Deno KV (`CACHE_BACKEND=kv`) or in-process MemoryKv (default). 292 tests passing. |
| 2026-06-20 | w0.7-verdict-cache | W0.7: KV-backed verdict cache keyed by (chain, address, schema_version). TTLs: 24h for safe_to_transact, 5min for do_not_transact, no cache for insufficient_data. Hit short-circuits before any oracle/service calls. Backed by Deno.Kv; memoryCache() for unit tests. |
| 2026-06-20 | bump-x402-timeout-5s | Bumped `DEFAULT_INVOKE_TIMEOUT_MS` from 2000ms → 5000ms. 2s was below the observed median x402 RTT (~4.2s/service on real wallets) and produced near-100% per-call timeouts; 5s gives services headroom without letting a hung host dominate the parallel fan-out. `INVOKE_TIMEOUT_MS` env still overrides. Per-request client-supplied `timeoutMs` was considered but deferred until W1.1 tenant auth lands so it can be gated. E2E verified against Vitalik fixture: 3/4 receipts ok (4.2s each), verdict `safe_to_transact`/high. |
| 2026-06-20 | w0.10-background-vetter-job | W0.10: background vetter cron job. `src/vetter/run.ts` probes every active/probation registry entry with a plain GET (no payment header), auto-bumps `price_usdc` to `real×1.20` when real price drifts above stored (within $0.10 ceiling), moves to probation above ceiling, propagates bumps to `data/call_recipes.json`, discovers new CDP candidates, and runs `recomputeScores()`. `scripts/vet.ts` is the manual-trigger entry point. `.github/workflows/vetter.yml` runs on a 12h GHA cron schedule. |
| 2026-06-20 | w0-final-sweep-prod-db | W0 sweep: confirmed all 10 tickets (W0.1–W0.10) shipped; fixed stale `**Status:**` headers (planned/done → completed) in five completed-plan files. Diagnosed why the Neon prod (main) branch saw no traffic — a single `DATABASE_URL` is reused everywhere and pointed at the dev branch. Documented the three-location dev/prod split in `docs/deployment.md` (local `.env`=dev; `deno-deploy` env secret + Deno Deploy dashboard=prod) and added target-host logging (no creds) to `scripts/migrate.ts` + `scripts/vet.ts`. Operational repointing (GH secret, dashboard var, prod migrate+seed) left as manual steps — gh token lacks secret-write and the auto-mode classifier blocked the prod migration. |
| 2026-06-20 | timeout-10s-and-llm-cost-line-item | Raised `DEFAULT_INVOKE_TIMEOUT_MS` 5s→10s (`INVOKE_TIMEOUT_MS` still overrides) to stop forcing timeouts on slow-but-valid x402 services. Added AI-model cost capture+display: `generateStructured` surfaces `agnic.cost_usd` via an `onCost` opt; `withCostTracking(llm, sink)` wraps the LLM client once in `verifyAgent` to accrue all pipeline LLM spend into `VerifyAgentResult.totalLlmCostUsd` (0 on cache-hit / oracle short-circuit); plumbed through both verify routes and the web result payload; `VerdictCard` shows `x402 services` + `AI model calls` subtotals and a combined `Total spent`. No cassette re-record (reads an already-recorded field). 318 backend tests pass. |
| 2026-06-20 | latency-shape-fast-tier-denylist | Latency-shape fix (no model pivot). Two-tier verify: `depth="fast"` runs only the free sanctions gate (denylist + Chainalysis oracle) and returns `fastSignal` block/proceed/needs_deep_check in <1s at $0; `depth="deep"` (default) is the full paid pipeline. New `$0` sanctioned denylist (`src/agent/sanctioned_denylist.ts`, long-TTL Deno KV) warmed by the vetter from the live OFAC SDN ETH list (`src/agent/ofac_list.ts`, 0xB10C mirror; local seed fallback) via `warmSanctionedDenylist()` + `scripts/warm-denylist.ts` + a workflow step — bounded by `|OFAC list|` (~93 today), TTL self-GCs. MCP made async: `verify_wallet` gains `depth` (default fast) + returns a `deepCheckToken`; new `get_deep_verdict` tool runs the paid check. Tiered UI: "Fast Check · $0" button + verdict-card `fast · $0` badge + opt-in "Run deep check · ~$0.03" CTA. 340 backend tests pass (+21), replay 9/9, web typecheck+build clean; fast tier verified end-to-end (block/proceed/needs_deep_check, all $0). |
| 2026-06-22 | cache-envelope-websentiment-tally | Three fixes on the 2-tier branch. (#2) Verdict cache now stores the full result envelope (`CachedVerdict`: verdict + receipts/outcomes + cost totals + walletNetwork, `SCHEMA_VERSION` 1→2) so a cache hit re-renders the paid-services breakdown identically to a fresh deep run; `VerifyAgentResult.fromCache` flags hits, UI shows "served from cache · $0 charged this run" and the header meter reads $0. (#3) web_sentiment timeouts: per-host concurrency cap of 2 in `invokeAll` (orbisapi labels+onchain+sentiment stop 429-ing each other), per-call `AbortSignal`+`timeoutMs` threaded into `agnicFetch` so the per-call timeout actually cancels the in-flight fetch, abortable rate-limit backoff, rate-limit failures canonicalised to `errorCode:"rate_limited"` (UI renders "rate-limited" not a misleading "timeout"), and web_sentiment demoted to best-effort (shorter 6s budget, non-blocking, rendered "skipped · best-effort"; still attempted). (#4) Header spend meter reconciled to grand total (x402 + AI) so it matches the card's "Total spent". No cassette re-record (no request-shape change). 345 backend tests pass (+4), replay 9/9, web typecheck+build clean. |
| 2026-06-28 | neon-mcp | Wired the Neon MCP server into `.mcp.json` (project-scoped `NEON_API_KEY`, proxied via `mcp-remote`) and added the `neon` + `neon-postgres` Claude skills. Read/inspect access only; schema changes still go through `db/migrations/*.sql` + `deno task db:migrate`. |
| 2026-06-28 | repo-drift-cleanup | Pre-feature drift sweep: corrected CLAUDE.md (MCP now 2 tools, LLM in `gateway.ts` not `clients/agnic.ts`, added `registry`/`cache`/`observability`/`vetter`/`testing` to module map, cut stale Agnic curl section, clarified `test`/`test:unit` aliases); centralized route error-dispatch into `src/routes/errors.ts` + shared `budgetThreshold` into `src/routes/budget.ts` (removed ~40 dup lines across 5 routes); added thin logger `src/observability/log.ts` replacing 41 scattered `console.*`; frontend dedup (canonical `web/src/utils.ts` `fmtUsd`, removed dead `clearLastPlan`). 360 backend tests (+15), replay 9/9, web typecheck+build clean. No cassette re-record (no request-shape change). |
