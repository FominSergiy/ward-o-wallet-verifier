# Agent Log

Append-only. One row per completed feature, newest at bottom. Cross-reference slug with `docs/features/<slug>.md`.

| Date       | Slug          | Summary                                                                         |
|------------|---------------|---------------------------------------------------------------------------------|
| 2026-05-21 | x402-payments | agnicFetch x402 proxy client + x402Invoker replacing all stubs in budgetedCall |
| 2026-05-21 | service-discovery | CDP x402 discovery pipeline + LLM rerank + POST /discover route + e2e test     |
| 2026-05-22 | pattern-adapter-shape-trim | Trim alternateBodyShapes 5→1 to cut paid-call volume; v3 e2e validation 2/2 match, 0 rate-limit hits |
| 2026-05-22 | durable-health-filter | Persist lastErrorCode in health store; rank filters services flagged with payment_exceeds_max (durable catalog↔runtime price drift) |
| 2026-05-22 | preflight-budget-check | /verify-agent calls Agnic /api/balance before running the DAG; returns HTTP 503 budget_exhausted if totalBalance < AGNIC_BUDGET_MIN_USD (default $0.10) |
| 2026-05-22 | legacy-rip-out | Delete /verify + /plan routes, src/dag/** stub DAG, USE_DISCOVERY legacy branch and 7 dependent modules; relocate shared types to src/agent/types.ts |
| 2026-05-23 | verify-agent-stream | Add VerifyEvent union + thread optional onEvent through verifyAgent / invokeAll / discover; POST /verify-agent-stream SSE route mirroring /verify-agent error mapping. Backend half of the demo-ui plan. |
| 2026-05-23 | demo-ui | Vite+React single-page demo under web/: Logo / InputForm / PlanCard (saved to localStorage) / LogStream (SSE) / VerdictCard. E2E verified in browser against vitalik.eth — Plan → Save → reload-restore → Execute → live phase/service events → "safe to transact" verdict. |
| 2026-05-23 | discover-stream | POST /discover-stream SSE route + two-tab terminal UI. Frontend Plan now streams discover events (phase/log/plan) into a "plan" tab, Execute into an "execute" tab — both preserved, click to flip without re-running. PlanEvent gained optional `unresolvedCategories`; api.ts extracted a generic `consumeSSE` helper. |
| 2026-05-23 | discovery-quality-and-not-found-fix | Refine labels/web_sentiment/contract_analysis CDP queries; EOA short-circuit drops contract_analysis when address has no bytecode (new `coverage.not_applicable` bucket); constrain LLM-fallback adapter to catalog URL path (validator + rewrite); normalize agnic error codes to snake_case and add `not_found` to durable-block codes. v6 e2e: 12/12 verdicts correct across 3 runs × 4 wallets, 0 `Not found` errors (v5 baseline: 30%). |
| 2026-05-23 | flow-viz | Pure-SVG flow diagram view on Execute tab — visualizes verify-agent fanout into categories → x402 payment diamonds → fallback diamonds (on primary error) → synthesize → verdict, derived from `VerifyEvent[]` via a `useFlowState` hook. Toggle (`logs ⇄ flow`) in the existing tab strip. |
| 2026-05-23 | v6-followups-and-orbis-probe | Wrap agnicFetch json-parse in try/catch (synthetic `non_json_response` code); add adapter_build_failed / adapter_call_failed / adapter_llm_build_failed codes; surface errorCode in route receipts; add dev-only FORCE_LLM_ADAPTER=true env flag + 2-wallet stress run (LLM path 8/8, 0 validator rewrites, verdicts still correct). Orbis probe revealed labels/reputation services were returning service-descriptor blobs, not address data — the pattern adapter is hitting the root URL when real data lives at `/label` and `/score` sub-paths. |
| 2026-05-23 | ui-wardo-styling | Ward-o mascot pass: hand-authored SVG mascot (neutral/safe/villain) in header + verdict card, plus pixel-art tamagotchi above the flow/logs panel — idle when waiting, sprite-sheet walk animation while plan or verify stream is active. |
| 2026-05-23 | adapter-descriptor-retry | Detect service-descriptor responses (`{endpoints[]}` shape) after a successful pattern call; retry once against the first non-info action sub-endpoint (category-hinted picker for labels/web_sentiment/sanctions), recording `adapterPath: "pattern+subpath"`. Validated end-to-end on 4 known wallets — both orbisapi labels + reputation services now hit 100% with substantive label/score payloads instead of API metadata; verdicts 4/4 match expected. |
| 2026-05-23 | pitch-deck-business | Business-side pitch deck at `docs/WARD-o-business.html` (alongside the existing technical deck). 9 slides, ~3-min runtime, story-led ("seatbelt for agents that spend money"), with ICP/persona, benefits-not-features cards, competitive 2×2, TAM/SAM/SOM, GTM, and ask. Speaker notes embedded with `?notes` toggle. Reuses palette + mascot SVGs from `assets/`. |
| 2026-05-23 | pitch-deck-workshop2 | Workshop #2 integration into the business deck: replaced Slide 6 (Why-Now tech timeline) with an adoption staircase (devs today → companies → consumer apps → mass-market crypto-via-AI), added Stripe/Twilio playbook one-liner to Slide 8 GTM. Speaker notes deepened on Slides 1/2/4/6/8 with real-world citations (Grok/Bankr, CoinDesk, TRM Labs), guardrail-as-backup framing, and Stripe/Twilio comparables. Deck stays at 9 slides; no time-budget impact. |
| 2026-05-23 | mcp-server | Expose `verifyAgent` as an MCP server with a single `verify_wallet` tool (address + chain + optional budgetCeiling/categories). Two transports share the same factory: stdio (`src/mcp/stdio.ts`) for local agent integrations and WebStandard Streamable HTTP (`src/mcp/http.ts`, port 9765) for hosted use. E2E verified via `scripts/mcp_e2e.ts` against vitalik.eth on eth — verdict `safe_to_transact`, $0.0174 USDC spent in 45s; HTTP `initialize` over curl returns server info + session id. |
| 2026-05-23 | ui-flow-polish | Flow view on plan tab (default flow on both tabs); flow diagram bumped to 480px (logs stay at 320px); cross-mode state flushed only when the wallet address changes — plan persists through execute for the same address. |
| 2026-05-23 | synthesis-signal-lift | Chainalysis sanctions-oracle on-chain hard-gate (chain primitive, free RPC read) + labels-discovery quality lift (sharper query, description-keyword rerank bump, post-call quality-probe with durable demotion) + viem ENS reverse resolution added to DEFAULT_CATEGORIES. T1 and T3 framed as chain primitives, not vendor URLs — verified BlockSec/MetaSleuth is not in CDP Bazaar, so pinning their URL was rejected to preserve the self-discovery pitch. v7 regression vs v6_3 baseline: 9/9 strict match, Lazarus 36s→2.7s and $0 spend, Vitalik/Binance medium→high confidence, no regressions. |
| 2026-05-23 | drop-chain-selector-and-surface-direct-paths | Drop UI chain selector; backend fans out Chainalysis oracle across all 5 EVM chains (strictest signal wins) and defaults other paths to eth. Fixes reported bug where `0x7F367cC41522cE07553e823bf3be79A889DEbe1B` returned safe on base but flagged on eth. Non-EVM addresses (Solana, Bitcoin) rejected with 400. Service events added to ENS resolver; `kind:"direct"` flag distinguishes free chain primitives from paid x402 calls. Flow diagram renders direct paths as dashed circles beneath each category. |
| 2026-05-23 | labels-registry-hybrid | Hybrid x402+hardcoded labels layer: `fetchLabelsRegistry` calls `https://eth-labels.com/labels/<address>` (dawsbot/eth-labels, free public API, ~170k labeled addresses) in parallel with the x402 labeler and ENS; result merges into `findings.labels` as `{x402_result, registry}` (same shape pattern as the Chainalysis oracle's merge into `findings.sanctions`). Synthesis prompt extended with CEX allow-list (coinbase, binance, kraken, …) and registry-negative blocklist (blocked, ofac-sanctioned, tornado-cash, …). v8 e2e vs v7: 9/9 strict match preserved, **Coinbase 1 and Kraken 4 lifted medium→high** with Opus reasoning explicitly citing registry attribution; spend down 38% ($0.1218→$0.0753); zero regressions across the other 7 fixtures. Both layers always run — registry is supplementary, never replaces self-discovery. |
| 2026-05-23 | ui-transparency-pass | UI now reflects what the agent actually does: PlanCard gained an "Always-on free checks" section showing Chainalysis oracle, eth-labels.com, ENS resolver at $0 with rationale tooltips (sourced from a new `buildDeterministicSources` builder; emitted via `DiscoveryPlan.deterministicSources` and the SSE `plan` event — no behavior change in `verify.ts`). VerdictCard receipts now render an `adapterPath` badge (`pattern`/`pattern+subpath`/`llm`) so the orbisapi descriptor-retry is visible. Every category chip in PlanCard and VerdictCard carries a native `title` tooltip from a new `web/src/categoryLabels.ts` hint map. End-to-end smoke on vitalik.eth: plan shows 5 paid + 3 free rows ($0.042 total), verdict shows pattern+subpath on the orbisapi labeler and llm on the web_sentiment fallback — verdict `safe_to_transact`. |
| 2026-05-23 | deploy-mcp-pipeline | mount MCP /mcp on main Hono app + bearer auth via MCP_SHARED_SECRET; restored on feat/deploy-pipeline so /mcp ships with the same Deno Deploy push as REST |
| 2026-05-24 | reliability-fixes-web-sentiment-ens-drop-contract-analysis | Drop `contract_analysis` category entirely (was always EOA-skipped or had no Bazaar provider); add `assertNoUnsubstitutedPlaceholders` URL validator in adapter so literal `:foo` placeholders fail fast as `unsubstituted_path_param` instead of going out as broken HTML 404s; expand `DURABLE_BLOCK_CODES` to add `unsubstituted_path_param` / `descriptor_only_response` / `non_json_response` for one-strike block on structural failures; surface ENS service events with `durationMs` + always-emitted `ens_resolve:` log line (LogStream renders `kind:"direct"` events as `resolved · {ms}` instead of `paid $0.0000`); add host-diversity hint in the rerank prompt + tighten `web_sentiment` / `onchain_history` queries to push the ranker off Orbis monoculture. E2E on vitalik.eth: bad Orbis `:endpoint` variant caught pre-call, alternate-rescue picks `wallet-address-risk-api-c6680c`, verdict `safe_to_transact` high-confidence, total spend $0.0109. |
| 2026-05-24 | ui-polish-flow-diagram | Cosmetic + correctness pass on the three demo cards. PlanCard: dashed divider + extra spacing between paid services and the "Always-on free checks" block. VerdictCard: three uppercase section headers (Summary / Category findings / Paid services breakdown) so the structure is scannable. FlowDiagram: bump `DIRECT_SPACING` 26→38 (chain labels no longer blur), wrap each sub-fan-out (sanctions 5 chains, ENS) in a dashed container rect so the row reads as one widget belonging to its parent category. Bug fixes: ENS edge no longer renders red on success — direct-only category status now promotes `active→ok` on the per-event handler and the invoke-end cascade correctly flips direct-only `active→ok` instead of `→error`. Fallback flow now renders as a single chain (`primary →(red)→ fallback →(green)→ synth`); the parallel `primary→synth` edge is suppressed entirely when a fallback exists. Verified against `0xdAD8…D96` (bob-the-builder.eth) with real backend: PlanCard divider visible, three verdict headers in DOM, ENS direct-node + edge both green, 15 edges total (down from 17 — the two suppressed are pay→synth when fallback exists). |
