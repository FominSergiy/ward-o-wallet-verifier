**What:** Rewrites health_store.ts to persist service health to the Postgres service_health_durable table; falls back to in-memory map when DATABASE_URL is unset so offline tests still pass.

**Files:** src/discovery/health_store.ts, src/discovery/health_store_test.ts, src/discovery/rank.ts, src/agent/invoke_all.ts

**Config:** DATABASE_URL must be set for persistence; HEALTH_TRACKING=false still disables tracking entirely.

**Notes:** All public health store functions are now async. No new migration needed — service_health_durable table was already created in 0001_init.sql. rank_test.ts and invoke_all_test.ts updated to use async health store APIs.
