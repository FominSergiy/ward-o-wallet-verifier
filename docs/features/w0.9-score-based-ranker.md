**What:** Deterministic score-based ranker that reads trailing-30-day `service_observations`, computes a reliability/latency/coverage score, and drives status transitions in `service_registry` — replacing any LLM rerank in the hot path.

**Files:**
- `src/registry/score.ts` (new) — `recomputeScores()` + pure helpers `computeScore()` / `nextStatus()`
- `src/registry/score_test.ts` (new) — 16 unit tests (all injectable seams, no DB required)
- `src/registry/select.ts` — explicit `status === "blocked"` guard in the candidate loop
- `src/registry/select_test.ts` — new "blocked service never appears regardless of score" test

**Config:** no new env vars; requires `DATABASE_URL` for the live DB path (same as the rest of the registry).

**Notes:**
- Score formula: `0.6 × reliability + 0.25 × latencyScore + 0.15 × coverageRate`. P95 latency at ≥30 s contributes 0; `null` latency (no observations) treated as perfect.
- Status transitions: active→probation (<50% reliability); probation→active (≥80%); probation→blocked (<20%); blocked is sticky — only an explicit operator action lifts it.
- `recomputeScores` is designed to be called on a schedule (cron/W0.10 vetter); not wired to a route yet.
- All three test-spec scenarios from the plan doc pass: 80%-failure demotion, perfect-score preservation, blocked exclusion from `selectFromRegistry`.
